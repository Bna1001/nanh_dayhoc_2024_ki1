@model IEnumerable<nanh_dayhoc_2024.Models.SinhVien>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HoTen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaLop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoTen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaLop)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaSinHVien">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaSinHVien">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaSinHVien">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
